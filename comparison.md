## Part E - Compare Human and AI Versions

| Question | Human version | AI version[cite: 1] |
| :--- | :--- | :--- |
| **Easy to understand?** | Yes, it uses basic lists and dictionaries. | Yes, it is very straightforward and uses the same basic logic. |
| **Runs successfully?** | Yes. | Yes, it executes without any syntax errors. |
| **Uses only required features?** | Yes. | Yes, it strictly avoided databases, GUIs, and external modules. |
| **Adds assumptions?** | No, just basic string storage. | No, it just stores the exact strings provided in the parameters. |
| **Handles errors?** | Partially (checks for an empty patient name). | No, it actually removed the blank name check that was in the human code. |
| **Could I explain it?** | Yes. | Yes, the code is very simple to follow and explain.
